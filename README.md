# Test001

A minimal repository containing a simple command-line calculator.

## Usage

Run `python calculator.py OPERATION X Y` where `OPERATION` is one of `add`, `sub`, `mul`, or `div`.

Example:

```
python calculator.py add 2 3
```

